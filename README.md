# Local development

## Getting started

### Install node modules

```shell
yarn install
```

Create a file in the root directory with name `env.development.local` and copy the content in `env.example` file.

### Run react app

```shell
yarn run dev
```
